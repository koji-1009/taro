name: taro
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/koji-1009/taro

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.0.0
  js: ^0.6.0
  path_provider: ^2.0.0
  quiver: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
